import React from 'react';
import './css/Tutorial.css';

export default () => (
  <div className="tutorial-container">
    <div>Each tab has 2 fields. Each field has a very loose validating algorithm attached to it. Except for the credit card field. It's picky.</div>
    <div>At least one field must be completed on each tab must be completed to continue to the next</div>
    <div>To begin the wizardry click the "Begin" button or click the first icon.</div>
  </div>
);